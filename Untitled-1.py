from tkinter import *

# widgets = GUI elements: buttons, textboxes, labels, images
# windows = seves as a containerto hold or contain these widgets

window = Tk() # will instansiate an instance of a window

window.mainloop()